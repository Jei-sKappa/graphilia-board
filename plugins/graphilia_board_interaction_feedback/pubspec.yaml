name: graphilia_board_interaction_feedback
description: "Plugin for graphilia_board_core that provides an abstact api for giving feedback to the user when interacting with the board."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.6.1
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  collection: ^1.19.0
  flutter:
    sdk: flutter
  graphilia_board_canvas_drawing: 0.0.1
  graphilia_board_core: 0.0.1
  graphilia_board_widget_drawing: 0.0.1
  value_notifier_tools: ^0.1.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
