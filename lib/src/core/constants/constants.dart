// TODO: Organize in different files
const highlighterOpactity = 0.5;

/// Default values for determine to draw a new point based on the distance from
/// the last point
const kDefaultPointDistance = 1.0;

/// Default values for determine to draw a new point based on the time from
/// the anchor point in a StraightLine
const kDefaultSnapTreshold = 10.0;

/// Default radius for resize rect's anchor
const kBaseResizeRectAnchorRadius = 6.0;
